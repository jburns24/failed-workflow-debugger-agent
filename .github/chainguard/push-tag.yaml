issuer: https://token.actions.githubusercontent.com
subject_pattern: repo:jburns24/failed-workflow-debugger-agent:ref:refs/heads/main
claim_pattern:
  workflow_ref: jburns24/failed-workflow-debugger-agent/.github/workflows/semantic-release.yaml@.* # Still not sure what needs to come after the actual workflow name but this works

permissions:
  contents: write